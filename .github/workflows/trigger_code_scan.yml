name: Trigger Code Scan

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  run-code-scan:
    uses: D5M4573R/dvja/.github/workflows/code-scan.yml@master
    with:
      project_key: "my-repo-project-key"
      sonar_org: "my-sonar-org"
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
